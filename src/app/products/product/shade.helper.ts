export interface ColorShades {
  featureTag: string;
  entityCode: string;
  pageNumber: string;
  filterTitle: {
    color_temperature: Array<string>;
    tonality: Array<string>;
    room: Array<string>;
  };
  entityName: string;
  popularity: string;
  shadeFamily: string;
  pageUrl: string;
  shadeHexCode: string;
  positionNumber: string;
  latest: string;
}
