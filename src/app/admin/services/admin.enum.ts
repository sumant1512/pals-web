export enum EUserType {
  ADMIN = 'Admin',
  DEALER = 'Dealer',
}

export enum ETransactionStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  SCANNED = 'scanned',
}

export enum ETransactionType {
  DEBIT = 'debit',
  CREDIT = 'credit',
}
